[pytest]
markers =
    integration: test de integración semi-real contra un backend + BD reales (crea y borra datos).
    destructive: test que además de crear también borra/fuerza estados (god-mode, hard-delete, etc.).
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning

